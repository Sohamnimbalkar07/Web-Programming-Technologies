function domaths(choice)
{ 
    function dosquare(a)
    {
        return a*a
    }
    function dopower(a,b)
    {    
        let d=Math.pow(a,b)
        return d
    }
    console.log(dopower(2,3))
    console.log(dosquare(2))
}
domaths()